VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# No VITE_ prefix — used server-side only by the /api serverless functions.
ANTHROPIC_API_KEY=
# Service-role key (Supabase dashboard: Project Settings > API > service_role).
# Bypasses RLS — used only by /api/_lib/auth.js, never sent to the browser.
SUPABASE_SERVICE_ROLE_KEY=
